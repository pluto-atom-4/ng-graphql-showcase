describe('BuildProgressCardComponent', () => {
  it.skip('deferred: component import causes vitest EISDIR resolution error', () => {
    // TODO: Debug vitest module resolution issue with BuildStatusService imports
    expect(true).toBe(true);
  });
});
