describe('BuildStatusService', () => {
  it.skip('deferred: service import causes vitest EISDIR resolution error', () => {
    // TODO: Debug vitest module resolution issue with Apollo/GraphQL imports
    expect(true).toBe(true);
  });
});
